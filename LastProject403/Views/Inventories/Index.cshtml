@model IEnumerable<LastProject403.Models.Inventories>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Straws.strawMaterial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.quantity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Straws.strawMaterial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.quantity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.inventoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.inventoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.inventoryID })
        </td>
    </tr>
}

</table>
