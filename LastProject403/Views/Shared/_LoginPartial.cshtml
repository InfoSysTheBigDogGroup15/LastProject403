@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    
    @Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">
    <li>
        @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "RealHome", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <li>
    <li>@Html.ActionLink("Logg off", "Create", "RealHome", null)</li></li>
</ul>
    
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Create", "RealHome", null)</li>
        <li>@Html.ActionLink("Log intest", "Login", "RealHome", null)</li>
    </ul>
}
