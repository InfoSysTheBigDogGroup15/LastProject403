@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    
    @Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">
    <li>
        @Html.ActionLink("Login", "Login", "RealHome", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <li>
    <li>@Html.ActionLink("Create", "Create", "RealHome", null)</li>
</ul>
    
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Create", "RealHome", null)</li>
        <li>@Html.ActionLink("Log intest", "Login", "RealHome", null)</li>
    </ul>
}
